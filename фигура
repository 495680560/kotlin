абстрактный класс Figure (var id: Int) {
    абстрактная забавная область (): Float
    абстрактный забавный ход (dx: Int, dy: Int)
    абстрактное веселье изменение размера (масштаб: Int)
    абстрактное развлечение повернуть (направление: RotateDirection, centerX: Int, centerY: Int)
}
